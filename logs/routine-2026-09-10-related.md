# 2026-09-10 · vocab-related (관련 용어 따라가기)

**후보 없음. 추가 0편.**

기존 단어의 `## 🔗 관련 용어` 에 나오는 이름이 전부 이미 자기 파일을 갖고 있다.
전략 문단의 지시대로 억지로 지어내지 않고 이 기록만 남기고 끝낸다.

## 권별 단어 수 (`python3 tools/build.py --dry-run`)

단어장 12권, 단어 **642개**

| 권 | 단어 수 |
|---|---|
| 컴퓨터과학 기초 | 43 |
| 프로그래밍 | 41 |
| 네트워크 | 76 |
| 웹 개발 | 35 |
| 데이터베이스 | 69 |
| 아키텍처 패턴 | 48 |
| 보안 · 인증 | 84 |
| 클라우드 | 61 |
| 인프라 · 운영 | 80 |
| 개발 도구 | 35 |
| AI · LLM | 42 |
| 제품 관리 | 28 |

빌드 제외(dropped) 0건.

## 후보 조사

두 가지 방법으로 따로 세어 서로 대조했다.

1. **build.py 의 실제 해석을 그대로 쓴 세기.**
   `collect()` → `assign_ids()` → `widen_aliases()` 로 만들어지는 alias 집합에,
   `parse_related()` 가 뽑은 관련 용어 링크를 소문자로 맞춰 대조했다.
   앱(`js/screens.js` 의 `termByName`)이 쓰는 판정과 같은 판정이다.

   - 관련 용어 섹션의 서로 다른 링크: **545개**
   - 그중 미해결(= 자기 노트가 없어 앱에서 회색 글자로 남는 것): **0개**

2. **파서를 믿지 않고 원문을 훑은 세기.**
   `parse_related()` 는 `- [[이름]]` 으로 시작하는 줄만 잡는다. 다른 모양으로
   쓰인 항목을 놓칠 수 있어서, 관련 용어 섹션의 원문에서 `[[...]]` 를 정규식으로
   전부 긁어 다시 대조했다. 결과는 같은 545개 · 미해결 0개였다.
   덤으로 `[[ ]]` 없이 맨 글자로만 적힌 관련 용어 항목도 찾아봤는데 **0줄**이다
   (즉 두 세기가 같은 것을 보고 있다).

검사에 쓴 스크립트는 임시 파일이라 저장소에 남기지 않았다. 위 두 문단이
그대로 재현 절차다.

### 조각 별칭 하나에 대한 확인 (지난 실행과 같음)

`widen_aliases`(복합 이름을 조각내 별칭에 더하는 후처리) 를 빼고 세면
`Materialized View` 하나가 미해결로 잡힌다. 하지만 이는
`content/데이터베이스/View & Materialized View.md` 가 `&` 기준으로 쪼개진
"materialized view" 별칭을 이미 갖고 있어서다. 앱에서도 눌리므로 후보가 아니다.
새 파일을 만들면 오히려 기존 노트와 이름이 부딪힌다.

## 검토했지만 버린 것

버린 후보가 따로 없다. 후보 풀 자체가 비어 있어서 3번(중복 배제)·4번(출처
조사) 단계로 넘어갈 대상이 없었다. 인용한 출처도 없다.

## 이 문서·도구에 대해 느낀 점

- **금요일 루틴이 네 번 중 세 번 빈손이다.** 08-20 0편, 08-27 1편, 09-03 0편,
  09-10(오늘) 0편. 구조적인 이유가 있다: 새 노트를 쓸 때 `🔗 관련 용어` 에는
  이미 있는 단어를 거는 것이 자연스럽고(그래야 앱에서 눌린다), 검사기도 없는
  단어를 걸라고 요구하지 않는다. 그래서 related 풀은 스스로 닫힌다. 월·수
  루틴이 단어를 넣어도 그 노트의 관련 용어가 다시 기존 단어를 가리키므로
  빈칸이 잘 안 생긴다. 사람이 금요일 자리를 다른 전략으로 바꾸는 것을
  한 번 생각해 볼 만하다 — 지금 상태로는 로그만 쌓인다.
  (판단은 사람 몫이라 `tools/` 는 건드리지 않았다.)
- **체크아웃이 origin/main 보다 한 커밋 뒤에 있었다.** 세션 시작 시 HEAD 가
  `2f2a014` 에 detached 상태였고 origin/main 은 `9f42f7d` 였다. 문서의 순서를
  그대로 따르면 1번 단계의 `--dry-run` 숫자와 후보 조사가 낡은 트리에서
  돌아가고, 8번 단계의 `git pull --rebase` 에 가서야 최신이 된다. 이번에는
  먼저 `git fetch origin main` 후 최신으로 옮겨 놓고 1번부터 다시 돌렸다.
  `ROUTINE-PROMPT.md` 1번 단계 앞에 "`git fetch` 로 최신을 확인하고 시작"
  한 줄이 있으면 좋겠다.
- 그 밖에 지시와 검사기가 어긋난 점은 이번 실행에서 발견하지 못했다.
